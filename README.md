# Model_scratch_log_loss-cross-entropy-loss-
This is a prediction model made from scratch which use libraries like(numpy, panda and maths) to predict wether a person can but/afford the insurance or not...

###This model is taking data from the provided insurance_data.csv
